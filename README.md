# CV-Agent (Hybrid IR + Grounded LLM Writing)

This project matches a candidate profile to a job description using TF-IDF, BM25, and optional embeddings, then generates grounded resume bullets and a cover letter.

## Run (Streamlit)
1) Create `.env` from `.env.example`
2) Install dependencies
3) Run:
```bash
streamlit run app.py
